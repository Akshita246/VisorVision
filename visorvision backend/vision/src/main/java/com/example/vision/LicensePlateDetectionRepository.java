package com.example.vision;

import org.springframework.data.jpa.repository.JpaRepository;

public interface LicensePlateDetectionRepository extends JpaRepository<LicensePlateDetection, Integer> {
}